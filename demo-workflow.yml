name: Demo Workflow

on:
  push:
    branches:
      - main

jobs:
  demo-job:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Show message
        run: echo "Demo workflow running on self-hosted runner!"
